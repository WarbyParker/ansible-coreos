To use the ansible-modules-extras submodule:

```
export ANSIBLE_LIBRARY=$PWD/ansible-modules-extras
```

Then `vagrant up` to spin up the vagrant box.

Make changes to `ansible-modules-extras/system/coreos.py`
